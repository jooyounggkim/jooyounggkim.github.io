# Jooyoung Kim

Source for [www.jooyounggkim.org](https://www.jooyounggkim.org), built with Jekyll and hosted by GitHub Pages.

## Local development

Install the Ruby dependencies, then run:

```sh
bundle exec jekyll serve --config _config.yml,_config.dev.yml
```
